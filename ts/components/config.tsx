import * as React from 'react';
import { ConfigState } from '../state';

interface ConfigProps {
    config: ConfigState;
}

export default class Config extends React.PureComponent<ConfigProps, {}> {
    render() {
        return <div>Not implemented yet</div>;
    }
}
